{
  "name": "salvo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Salvo — a touch-first artillery duel across destructible terrain.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "icons": "node scripts/gen-icons.mjs",
    "standalone": "node scripts/build-standalone.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vite-plugin-pwa": "^0.20.5"
  }
}
