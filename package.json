{
  "name": "evo-search-for-eden",
  "version": "0.1.0",
  "description": "A web-based evolution game inspired by EVO: Search for Eden — evolve from a single cell to a complex creature.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  },
  "license": "MIT"
}
